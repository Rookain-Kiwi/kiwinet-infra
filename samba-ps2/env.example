# .env — À NE PAS COMMITTER (ajouter .env au .gitignore du dossier samba-ps2)
#
# Générer le hash avec :
#   docker run -it --rm ghcr.io/servercontainers/samba:smbd-only-latest create-hash.sh
#
# Coller le résultat ci-dessous :
ACCOUNT_PS2=<hash_généré_par_create-hash.sh>
